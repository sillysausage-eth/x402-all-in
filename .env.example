# ===========================================
# Agent All In - Environment Variables
# ===========================================
# Copy this file to .env.local and fill in your values
# Never commit .env.local to version control!

# -------------------------------------------
# Supabase Configuration
# -------------------------------------------
# Your Supabase project URL (found in Settings > API)
NEXT_PUBLIC_SUPABASE_URL=

# Supabase publishable/anon key (safe for client-side)
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Supabase service role key (server-side only, keep secret!)
SUPABASE_SECRET_KEY=

# -------------------------------------------
# Anthropic (Claude AI)
# -------------------------------------------
# Your Anthropic API key for AI agent decisions
# Get one at: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# -------------------------------------------
# Thirdweb Configuration
# -------------------------------------------
# Thirdweb client ID for wallet connections
# Get one at: https://thirdweb.com/dashboard
NEXT_PUBLIC_THIRDWEB_CLIENT_ID=

# Thirdweb secret key for server-side operations (Server Wallets)
# Required for: game creation, seeding pools, resolving games
THIRDWEB_SECRET_KEY=

# -------------------------------------------
# Chain/Network Environment
# -------------------------------------------
# IMPORTANT: This controls which network and contracts the app uses
#
# Options:
#   - "development" (default) = Base Sepolia testnet + MockUSDC
#   - "production" = Base mainnet + real Circle USDC
#
# All contract addresses are defined in: src/lib/contracts/config.ts
# That file is the SINGLE SOURCE OF TRUTH for all addresses
#
# For localhost/dev deployments: use "development"
# For production deployments: use "production"
NEXT_PUBLIC_CHAIN_ENV=development
